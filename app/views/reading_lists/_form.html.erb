<%= form_for @reading_list, html: { multipart: true} do |f| %>
  <% if @reading_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reading_list.errors.count, "error") %> prohibited this reading_list from being saved:</h2>

      <ul>
      <% @read.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>

 <!-- needs work, to allow the user to pick articles to add to his reading list -->


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
